apiVersion: v1
kind: Service
metadata:
  name: counter-backend
spec:
  ports:
    - name: http
      port: {{ .Values.backend.service.port }}
      protocol: TCP
      targetPort: http
  selector:
    app.kubernetes.io/component: backend
