apiVersion: v1
kind: Service
metadata:
  name: counter-frontend
spec:
  ports:
    - name: http
      port: {{ .Values.frontend.service.port }}
      protocol: TCP
      targetPort: http
  selector:
    app.kubernetes.io/component: frontend
